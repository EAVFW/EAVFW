/** Configuration for a single ribbon button (visibility, disabled state, quick-create support). */
export type RibbonViewItemInfo = {
  visible?: boolean;
  disabled?: boolean;
  supportQuickCreate?: boolean;
  visibleOnCard?: boolean;
};
