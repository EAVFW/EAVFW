import { PrimitiveType } from './PrimitiveType';

/** Object wrapper around a {@link PrimitiveType} string, used inside {@link NestedType}. */
export type PrimitiveTypeDefinition = {
  type: PrimitiveType;
};
